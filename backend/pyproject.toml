[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.121.0",
    "pydantic>=2.12.4",
    "pytest>=8.4.2",
    "uvicorn>=0.38.0",
]
[tool.pytest.ini_options]
pythonpath = [
  "."
]